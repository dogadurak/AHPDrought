[pytest]
pythonpath = .
testpaths = tests
addopts = -ra -q
markers =
    network: dış servislere bağlanan testler. Varsayılan olarak çalışır,
        bağlantı yoksa atlanır. Tamamen kapatmak için: pytest -m "not network"
